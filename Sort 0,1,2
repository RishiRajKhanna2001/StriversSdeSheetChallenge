#include<bits/stdc++.h> 
void sort012(int *arr, int n)
{
int s=0;
	int  m=0;
	int e=n-1;
	
	while(s<=m && m<=e)
	{

		if(arr[m]==0)
		{
			swap(arr[s],arr[m]);
			s++;
			m++;
		}
		else if(arr[m]==2)
		{
			swap(arr[e],arr[m]);
			e--;
		}
		else
		{
			m++;
		}
	}
}
